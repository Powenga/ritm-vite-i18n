export type TDataFeedback = {
  userName: string;
  userPhone: string;
  userEmail: string;
  userMessage?: string;
  policy: boolean;
};

export type Langs = {
  [key: string]: { nativeName: string };
};


